apiVersion: apps/v1
kind: Deployment
metadata:
  name: app
  labels:
    app: app
spec:
  replicas: 3
  selector:
    matchLabels:
      app: app
  template:
    metadata:
      labels:
        app: app
    spec:
      containers:
      - name: app
        image: vagneralves1/kubernetes-projetofinal:1.1
        imagePullPolicy: Always
        ports:
        - containerPort: 80
